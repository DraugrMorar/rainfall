Заходим в программу под правами пользователя level0 с паролем level0.
У нас есть бинарный файл level0, в gdb делаем disass main и видим:

   0x08048ed4 <+20>:	call   0x8049710 <atoi>         вызов функции atoi, которая конвертирует начальную часть строки,
                                                        полученной из стандартного потока ввода, в целое число.
   0x08048ed9 <+25>:	cmp    $0x1a7,%eax              далее происходит сравнение (cmp) c числом 423.
   0x08048ede <+30>:	jne    0x8048f58 <main+152>     команда jne(переход, если не равно) переход на строку позже вызова функции execv.


Далее вызовы других функций:

   0x08048ee7 <+39>:	call   0x8050bf0 <strdup>       возвращает указатель на новую строку, являющуюся дубликатом строки
   0x08048ef8 <+56>:	call   0x8054680 <getegid>      возвращает фактический идентификатор ID пользователя в текущем процессе
   0x08048f01 <+65>:	call   0x8054670 <geteuid>      возвращает фактический идентификатор GID пользователя в текущем процессе
   0x08048f21 <+97>:	call   0x8054700 <setresgid>    устанавливает идентификатор реального пользователя, идентификатор эффективного
                                                        пользователя и идентификатор сохраненного пользователя текущего процесса. 
   0x08048f3d <+125>:	call   0x8054690 <setresuid>    устанавливает идентификаторы групп реальных, эффективных и сохраненных пользователей процесса.  
   0x08048f51 <+145>:	call   0x8054640 <execv>        заменяет текущий образ процесса новым образом процесса
   0x08048f7b <+187>:	call   0x804a230 <fwrite>       записывает элементы данных в поток (в нашем случае строку: "No !")


Соответственно нам необходимо вызвать функцию execv, которая вызовет новый образ процесса,
в котором у нас будут права пользователя level1 и мы сможем прочитать файл .pass.
Самый простой и очевидный способ ввести то число, с которым идет сравнение (cmp    $0x1a7,%eax) то есть  423.

> ./level0 423
$> id
uid=2030(level1) gid=2020(level0) groups=2030(level1),100(users),2020(level0)    Мы попадаем в другой образ процесса,
                                                                                 где наши идентификаторы изменены и мы имеем права юзера level1.
$> cat /home/user/level1/.pass                                                   Можем посмотреть содержимое файла .pass 
1fe8a524fa4bec01ca4ea2a869af2a02260d4a7d5fe7e7c24d8617e6dca12d3a                 А вот и наш пароль